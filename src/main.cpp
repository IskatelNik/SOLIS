#include "Core/Game.h"

/**
 * @brief Точка входа в приложение SOLIS.
 * Создает экземпляр игры и запускает главный цикл.
 */
int main() {
    // Инициализация игры с разрешением Full HD
    solis::Game game("SOLIS", 1920, 1080);
    
    // Запуск основного цикла обработки
    game.run();
    
    return 0;
}
